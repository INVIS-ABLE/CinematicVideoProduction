# Placeholder for the packaged Windows installer build (Phase: installable app).
# The current install path is windows_one_click_setup.ps1 (venv-based).
# This script will assemble a self-contained distribution (embedded Python,
# wheels cache, shortcuts) once the engine phases stabilise — tracked in
# fable_memory/roadmap.md.
Write-Host "Packaged installer build is a roadmap item; use windows_one_click_setup.ps1."
exit 1
